name: order
schema: dbo
columns: 
  Id: 
    Identity: true
    Type: int
    Description: Holds information about orders
    nullable: false
  CustomerId:
    Type: int
    References: customer
    Description: Points to the customer table
    nullable: false
    
index:
  IDX_BLA_BLA:
    columns:
      - CustomerId

# referencedIn:
#   - orderItem:
#     column: orderId
