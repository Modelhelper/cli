key: file
type: api
version: 3.0

short: Read files from a folder, can be json or yaml. Content is structures as a database entity (table)
description: |
  Use mssql or postgres datatypes for the list of columns

